describe('Test GET /newspapers', () => {
    test('It should respond with 200 success', () => {
        const response = 200;
        expect(response).toBe(200);
    })
})

describe('Test POST /newspapers', () => {
    test('It should respond with 200 sucess', () => {
        const response = 200;
        expect(response).toBe(200);
    })
})